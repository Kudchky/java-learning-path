package com.polcastillo.fundamentals.strings;

public class StringCharAtExercise {
    void main() {
        String word = "Programar";

        IO.println(word.charAt(0) + " " + word.charAt(word.length() - 1));
    }
}
